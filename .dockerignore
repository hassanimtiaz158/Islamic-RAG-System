# Version control
.git
.gitignore

# Environment / secrets
.env
.env.*
.env.local

# Python
__pycache__
*.py[cod]
*$py.class
*.so
*.egg-info
dist/
build/
*.egg

# Virtual environments
venv/
.venv/
env/

# Data (vector store is 400MB+ and gitignored — never copy into image)
data/
vectorstore/
vectorstore*/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# DVC
.dvc/cache
.dvc/tmp
*.dvc

# Logs
*.log

# Tests / scripts (not needed in production image at runtime; indexing is
# performed out-of-band and the app degrades gracefully to demo mode).
scripts/
tests/

# Frontend IS served by the backend container (see Dockerfile COPY frontend/
# and the StaticFiles mount in src/api/main.py). Keep it in the build context.

# Docker meta
Dockerfile
docker-compose.yml
.dockerignore

# Misc
*.md
!README.md
nul
touch
.claude/
.vercel/
.vercel.json
